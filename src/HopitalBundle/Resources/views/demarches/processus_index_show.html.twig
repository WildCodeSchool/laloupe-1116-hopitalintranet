{% extends 'base.html.twig' %}

{% block body %}


    <div class="row">
        <div class="col-md-offset-2 col-md-8 fondblancshow">
            <h1 class="lienstitre">{{ categorie.categorie }}</h1>
            {% for processus in processuss if processus.categorie.id == categorie.id %}
                <div class="col-xs-6 instancesimg" >
                    <object class="img-thumbnail" width="100%" height="auto" data="{{ app.request.basepath }}/uploads/{{ processus.processus1 }}"></object>
                    <a id="instanceslink" data-toggle="modal" data-target="{{ '#' ~ processus.id }}">Agrandir</a>
                    <div id="{{ processus.id }}" class="modal" role="dialog">
                        <div class="modal-dialog modal-lg">
                            <!-- Modal content-->
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                                </div>
                                <div class="modal-body">
                                    <div class="col-xs-12 instancesimg2">
                                        <object class="img-responsive" width="100%" height="auto" data="{{ app.request.basepath }}/uploads/{{ processus.processus1 }}"></object>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            {% endfor %}


        </div>
    </div>




{% endblock %}