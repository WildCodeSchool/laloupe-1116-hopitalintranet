{% extends 'base.html.twig' %}

{% block body %}
    <h1>Certification</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ certification.id }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('demarches_certification_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('demarches_certification_edit', { 'id': certification.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(certification_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(certification_delete_form) }}
        </li>
    </ul>
{% endblock %}
