{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2  col-md-8 fondblanc">

            <h1 class="lienstitre">Pages jaunes</h1>
            <h4 class="lienssoustitre">Annuaire des professionnels</h4>
            <a href="{{ asset('http://www.pagesjaunes.fr/') }}"><img src={{ asset('bundles/hopital/img/PagesJaunes.png') }} alt="PagesJaunes" class="lienimg"/></a>

            <a  href="{{ asset('http://www.pagesjaunes.fr/') }}"><button type="button" class="btn btn-primary">Site des pages jaunes</button></a>

        </div>
    </div>


    <a href="{{ asset('http://www.pagesjaunes.fr/') }}">Pages jaunes</a></br>

    {#<table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for recommandation in recommandations %}
            <tr>
                <td><a href="{{ path('recommandation_show', { 'id': recommandation.id }) }}">{{ recommandation.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('recommandation_show', { 'id': recommandation.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('recommandation_edit', { 'id': recommandation.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recommandation_new') }}">Create a new recommandation</a>
        </li>
    </ul>#}
{% endblock %}
