{% extends 'base.html.twig' %}

{% block body %}
    <h1>HAS</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ has.id }}</td>
        </tr>
        <tr>
            <th>hasimg</th>
            <td>{{ has.hasimg }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recommandation_has_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recommandation_has_edit', { 'id': has.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(has_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(has_delete_form) }}
        </li>
    </ul>
{% endblock %}
