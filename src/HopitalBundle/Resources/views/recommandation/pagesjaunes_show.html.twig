{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pages Jaunes</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ pagesjaunes.id }}</td>
        </tr>
        <tr>
            <th>pagesjaunesimg</th>
            <td>{{ pagesjaunes.pagesjaunesimg }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recommandation_pagesjaunes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recommandation_pagesjaunes_edit', { 'id': pagesjaunes.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(pagesjaunes_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(pagesjaunes_delete_form) }}
        </li>
    </ul>
{% endblock %}
