{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2 col-md-8 fondblanc">
            <h1 class="lienstitre">Création Journal officiel</h1>
            <br>
            <div class="row journaux">
                <div class="col-md-4">
                    <br>
                    <br>
                    <br>
                    <br>
                    {{ form_start(form) }}
                    {{ form_label(form. journalofftitle, 'Nommez votre lien:', {'journalofftitle': {'class': 'journalofftitle'}}) }}
                    {{ form_widget(form.journalofftitle)}}
                    {{ form_label(form. journaloffdescription, 'Décrivez votre lien:', {'journaloffescription': {'class': 'journaloffdescription'}}) }}
                    {{ form_widget(form.journaloffdescription)}}
                    {{ form_label(form. journaloffimg, 'Nommez votre image:', {'journaloffimg': {'class': 'journaloffimg'}}) }}
                    {{ form_widget(form.journaloffimg)}}
                    {{ form_label(form.file1, 'Insérez votre image:', {'file1': {'class': 'file1'}}) }}
                    {{ form_widget(form.file1)}}
                    {{ form_label(form. journaloffurl, 'Indiquez le lien:', {'journaloffurl': {'class': 'journaloffurl'}}) }}
                    {{ form_widget(form.journaloffurl)}}
                    <br>
                    <input type="submit" class="btn btn-primary btn-lg" value="Créer" />
                    {{ form_end(form) }}
                </div>
            </div>

        </div>
    </div>
{% endblock %}