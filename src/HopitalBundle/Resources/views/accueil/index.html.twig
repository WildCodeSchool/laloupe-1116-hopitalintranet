{% extends 'base.html.twig' %}

{% block body %}
    <body>
<main>
    {% for accueil in accueils %}
        <div class="row">
            <div class="col-md-6" id="nouveautes">
                <ul id="news">
                    <li>Consulter la nouvelle <a href="">lettre d'information</a> de Chartres.</li>
                    <li>Reservez vos places à <a href="">DisneyLand Paris</a>.</li>
                    <li>Osrganisez votre <a href="">groupe de travail</a>.</li>
                    <li>Consultez les <a href="">postes vacants</a> pour de nouvelles opportunités.</li>
                </ul>
            </div>
            <div class="col-md-4">
                <div class="container" id="search">
                    <div class="input-group" id="adv-search">
                        <input type="text" class="form-control" placeholder="Rechercher" />
                        <div class="input-group-btn">
                            <div class="btn-group" role="group">
                                <button type="button" class="btn btn-primary"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 actu">
                <div class="jumbotron">
                    <div class="container">
                        <h3>ACTUALITÉS</h3>
                        <p class="actuliens">
                            <script type="text/javascript" src="{{ accueil.actuurl }}" data-fw-param="{{ accueil.actuparam }}">

                            </script>
                    </div>
                </div>
            </div>
            <div class="col-md-4 liensUtiles">
                <div class="jumbotron">
                    <div class="container utiles">
                        <h3>LIENS UTILES</h3>
                        <a href="{{ path('moncompte_agenda') }}">- Calendrier des réservations</a><br>
                        <a href="{{ accueil.dgsurl}}" target="_blank">{{ accueil.dgstitle }}</a><br>
                        <a href="{{ accueil.bluemediurl}}" target="_blank">{{ accueil.bluemedititle }}</a><br>
                        <a href="{{ accueil.viatrajectoirurl}}" target="_blank">{{ accueil.viatrajectoirtitle }}</a><br>
                        <a href="{{ accueil.vidalurl}}" target="_blank">{{ accueil.vidaltitle }}</a><br>

                    </div>
                </div>
            </div>
            <div class="col-md-4 meteo">
                <div class="jumbotron">
                    <div class="container">
                        <p class="alerte"><iframe src="{{ accueil.meteourl }}"  width="380" height="111" frameborder="0">
                            </iframe></p>
                    </div>
                </div>
            </div>
        </div>
        </main>

        </body>
    {% endfor %}
{% endblock %}
