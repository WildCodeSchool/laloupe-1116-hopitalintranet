{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2 col-md-8 fondblanc">
            <h1 class="lienstitre">Journaux internes edit</h1>
            {{ form_start(edit_form) }}
            <div class="row journaux">
                <div class="col-md-4">
                    <object data="{{ asset('bundles/hopital/img/journaux/16_07FlashInfo.pdf') }}" type="application/pdf" class="journalimg"></object>
                    {{edit_form.children.file1.vars.label}}
                    {{ form_widget(edit_form.file1) }}
                    {{ form_errors(edit_form.file1) }}
                    {{ form_widget(edit_form.journauximg) }}
                    {{ form_errors(edit_form.journauximg) }}
                </div>
                <li>
                    <a href="{{ path('documentation_journaux_new') }}">Ajouter un journal</a>
                </li>
                {{ form_widget(edit_form) }}

                <input type="submit" value="Edit"/>

                {{ form_end(edit_form) }}

                <ul>
                    <li>
                        <a href="{{ path('documentation_journaux_index') }}">Back to the list</a>
                    </li>
                    <li>
                        {{ form_start(journaux_delete_form) }}
                        <input type="submit" value="Delete">
                        {{ form_end(journaux_delete_form) }}
                    </li>
                </ul>




                {{ form_end(edit_form) }}


                <br/>
                <br/>



{% endblock %}
