{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2 col-md-8 fondblanc">
            <h1 class="lienstitre">Création de galerie</h1>
            <br>
            <div class="row basedoc">
                <div class="col-md-4">
                    <br>
                    <br>
                    <br>
                    <br>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Create" />
                    {{ form_end(form) }}
                </div>
            </div>

        </div>
    </div>


    <ul>
        <li>
            <a href="{{ path('documentation_galerie_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
