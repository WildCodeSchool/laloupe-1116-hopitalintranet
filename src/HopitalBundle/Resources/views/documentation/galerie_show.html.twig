{% extends 'base.html.twig' %}

{% block body %}
    <h1>Documentation</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ galerie.id }}</td>
        </tr>
        <tr>
            <th>Galerie1</th>
            <img src="{{ app.request.basepath }}/uploads/{{ galerie.galerie1 }}"/>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('documentation_galerie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('documentation_galerie_edit', { 'id': galerie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}