{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2  col-md-8 fondblanc">
            <h1 class="lienstitre">Direction commune</h1>
            <object data="{{ asset('bundles/hopital/img/DirectionCommune.pdf') }}" type="application/pdf" class="directioncommune">
            </object>
        </div>
    </div>



    {#<table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for communication in communications %}
            <tr>
                <td><a href="{{ path('communication_show', { 'id': communication.id }) }}">{{ communication.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('communication_show', { 'id': communication.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('communication_edit', { 'id': communication.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('communication_new') }}">Create a new communication</a>
        </li>
    </ul>#}
{% endblock %}
