{% extends 'base.html.twig' %}

{% block body %}
    <h1>GHT</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ ght.id }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('communication_ght_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('communication_ght_edit', { 'id': ght.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(ght_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(ght_delete_form) }}
        </li>
    </ul>
{% endblock %}
