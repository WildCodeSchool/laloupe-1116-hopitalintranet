{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liens des sites internet des établissements</h1>




    <a href="{{ asset('http://www.ch-chartres.fr') }}">CHARTRES</a></br>
    <a href="{{ asset('http://www.ch-dreux.fr/') }}">DREUX</a></br>
    <a href="{{ asset( 'http://www.ch-chateaudun.fr/') }}">CHATEAUDUN</a></br>
    <a href="{{ asset('https://www.ch-nogent-le-rotrou.fr/') }}">NOGENT LE ROTROU</a></br>
    <a href="{{ asset( 'http://www.ch-henriey.fr/') }}">BONNEVAL</a>
    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for communication in communications %}
            <tr>
                <td><a href="{{ path('communication_show', { 'id': communication.id }) }}">{{ communication.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('communication_show', { 'id': communication.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('communication_edit', { 'id': communication.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('communication_new') }}">Create a new communication</a>
        </li>
    </ul>
{% endblock %}
