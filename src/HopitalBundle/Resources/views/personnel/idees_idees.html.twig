{% extends 'base.html.twig' %}

{% block body %}


    <tbody>
    {% for idees in idees %}

        <div class="row">
            <div id="page-contact">
                <div class="col-md-offset-2  col-md-8 fondblanc">
                    <h1 class="lienstitre">Boîte à idées</h1>
                    <p>Votre message a été envoyé avec succès</p>
                    <button href="{{ path('personnel_idees_index') }}" type="button" class="btn btn-primary btn-lg">
                        Retours à la page boîte à idées
                    </button>
                </div>
            </div>
        </div>

    {% endfor %}
    </tbody>

{% endblock %}
