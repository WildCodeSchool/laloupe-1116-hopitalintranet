{% extends 'base.html.twig' %}

{% block body %}
    <h1>Idées</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ idees.id }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('personnel_idees_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('personnel_idees_edit', { 'id': idees.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(idees_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(idees_delete_form) }}
        </li>
    </ul>
{% endblock %}
