{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-offset-2 col-md-8 fondblanc">
            <h1 class="lienstitrenew">Modification d'une page co-voiturage</h1>
            <br>
            <div class="row">
                <div class="col-md-offset-3 col-md-6 carreeditid">
                    <div class="row">
                        <div class="col-md-12 imgnew">
                            {{ form_start(edit_form) }}
                            <a href="{{ app.request.basepath }}/uploads/{{ covoiturage.covoiturageimg }}" >Votre image</a>
                            {{edit_form.children.file1.vars.label}}<br><br>
                            {{ form_widget(edit_form.file1) }}<br>
                            {{ form_errors(edit_form.file1) }}
                            {{ form_widget(edit_form.covoiturageimg) }}<br><br>
                            {{ form_errors(edit_form.covoiturageimg) }}
                            {{ form_widget(edit_form) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-offset-5 col-md-2 btneditcommun">
                            <input type="submit" class="btnedit" value="Modifier"/>
                            {{ form_end(edit_form) }}
                            {{ form_start(covoiturage_delete_form) }}
                            <input type="submit" class="btnedit" value="Supprimer">
                            {{ form_end(covoiturage_delete_form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}


