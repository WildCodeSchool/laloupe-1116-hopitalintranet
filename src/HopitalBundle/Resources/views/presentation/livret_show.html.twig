{% extends 'base.html.twig' %}

{% block body %}
    <h1>Livret</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ livret.id }}</td>
        </tr>
        <tr>
            <th>livretimg</th>
            <td>{{ livret.livretimg }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('presentation_livret_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('presentation_livret_edit', { 'id': livret.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(livret_delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(livret_delete_form) }}
        </li>
    </ul>
{% endblock %}
